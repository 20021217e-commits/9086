// *****************************************************************************
//  版权所有  (C) 2015,  硅格半导体有限公司 
//   
//  文件名称： irq.c
//  文件标识：  
//  内容摘要：  所有应用层中断实现代码
//  其它说明：  
//  当前版本：  v0.10
//  项    目：  SG9081
//  作    者：  William
//  完成日期：  2016年07月22日
//   
//  修改记录 1：修改历史记录，包括修改日期、修改者及修改内容 
//  修改日期： 
//  版 本 号： 
//  修 改 人： 
//  修改内容：   
// *****************************************************************************

// *****************************************************************************
// 函数名称: FC0_CQ_IRQHandler
// 函数功能: CQ中断服务函数
// 入口参数: NONE
// 出口参数:
// 说    明:
// *****************************************************************************
#include "sg9081.h"
#ifndef __G3_FLYCODE
void FC0_CQ_IRQHandler(void)
{
	/*
	U8 bCh;
	for (bCh=0; bCh<4; bCh++)
	{
		if (0 != SG_FC(bCh)->rCHK_INT_ST.dAll)
		{
			if(SG_FC(bCh)->rCHK_INT_ST.bits.CQ_INT_FLAG)
			{
				SG_FC(bCh)->rCHK_INT_ST.bits.CQ_INT_FLAG =0;
			}

			if(SG_FC(bCh)->rCHK_INT_ST.bits.ECC_OVERFLOW_INT_FLAG)
			{
				SG_FC(bCh)->rCHK_INT_ST.bits.ECC_OVERFLOW_INT_FLAG =0;
			}

			//            if(SG_FC(bCh)->rCHK_INT_ST.bits.ECC_FAIL_INT_FLAG)
			//            {
			//                SG_FC(bCh)->rCHK_INT_ST.bits.ECC_FAIL_INT_FLAG =0;
			//            }

			if(SG_FC(bCh)->rCHK_INT_ST.bits.CQ_STOP_FLAG)
			{
				SG_FC(bCh)->rCQ_CTRL.dAll = 0x02;//CQ_CLEAR写1清0
				SG_FC(bCh)->rCQ_CTRL.dAll = 0x00;//CQ_CLEAR写1清0
				SG_FC(bCh)->rCHK_INT_ST.bits.CQ_STOP_FLAG =0;
			}
			if (SG_FC(bCh)->rCHK_INT_ST.bits.CQ_STOP_ST_RB_CQ_FLAG)
			{
				SG_FC(bCh)->rCQ_CTRL.bits.CQ_CLEAR = 1;
			}
		}
	}
	*/
}
#endif


